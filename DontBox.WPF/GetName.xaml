<Window x:Class="DontBox.WPF.GetName"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GetName" Height="106.985" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.ColumnSpan="2" TextAlignment="Center" VerticalAlignment="Center">Please give me a name</TextBlock>
        <TextBox HorizontalAlignment="Stretch" Margin="20,0" VerticalAlignment="Center" Grid.ColumnSpan="2" Grid.Row="1" x:Name="namey" Text="{Binding Name}"/>
        <Button Style="{StaticResource BS}" Margin="0,5" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" Click="Ok_Click">Ok</Button>
        <Button Style="{StaticResource BS}" Margin="52,5,54,5" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Click="Cancel_Click">Cancel</Button>
    </Grid>
</Window>
